using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioScript : MonoBehaviour
{

    //public AudioSource[] rSounds;

    //public AudioSource rSound1;
    //public AudioSource rSound2;
    //public AudioSource rSound3;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       // rSounds[Random.Range(0, rSounds.Length)].Play();
    }
}
